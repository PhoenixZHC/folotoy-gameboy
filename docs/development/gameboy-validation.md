<p align="right"><a href="gameboy-validation.zh_CN.md">简体中文</a> · <strong>English</strong></p>

# Game Boy Port Validation

For the current rollback baseline, compatibility matrix, and acceptance limits, see [baseline acceptance](gameboy-acceptance.md). The table below retains measurements from earlier implementation stages; individual historical checks do not establish acceptance of every subsequent build.

The prototype was flashed to an ESP32-C3 on COM14 on 2026-09-17. Report build, host, and device results independently. The current build uses ESP-IDF 5.5.3 and an 8 MB Flash layout without official or recovery partitions.

| Check | Status | Evidence or next measurement |
| --- | --- | --- |
| Partition and application size | Passed in build | ESP-IDF 5.5.3 build, esptool merged-image generation, and `tools/verify_firmware.py` passed. Current app `2509968 / 3145728` bytes; final partition ends at `0x800000`. |
| Clean 8 MiB image from current application | Packaging and verification passed on 2026-09-24 | `artifacts/releases/FoloToy-GameBoy-8MB-clean.bin` is exactly 8,388,608 bytes. The release packager verified image layout, erased NVS/PHY/ROM regions, and reproducible initial saves with no game progress; independent SHA-256 and its `.sha256` sidecar agree on `cc4f8bc8c988f884c2233048edc3cf4d715fc49f8393bcd4b2b4decbafb93c12`. The app within this image is 2,509,968 bytes. This clean image was not flashed to the device. |
| ROM package and partition checks | 4 host tests passed | ROM round trip, corruption, unsupported mapper, truncation, and exact five-partition layout. |
| Firmware readback verifier | 12 host tests passed | Immutable images must match; NVS may change after boot without causing a false failure. |
| First install and ROM import on COM14 | Passed image verification | First-install wrote the merged firmware and initial saves. At that stage, the ROM-only image at `0x310000` contained 2048 and the user-provided Pokemon Blue; its 1,085,440 bytes were read back and matched. The previous 2048 + Pirate's Folly image also passed readback before replacement. Bootloader, partition table, app, ROM, and saves were verified after first install. NVS changes during boot and is deliberately excluded from immutable-image comparison. |
| Boot and BLE scanning | Passed on device | The device boots without panic, active BLE scanning finds the Xbox advertisement, and the physical screen shows a selectable gamepad. The log also reports HCI opcode `0x0c05` status 1; its effect is not yet established. |
| Input, pixel, and renderer logic | 4 host programs passed | Candidate filtering/selection, direction conflicts/disconnect, grayscale packing/scaling, and randomized background/window rendering against a per-pixel reference. |
| Xbox 1914 BLE pairing and input | Passed basic device test | Firmware 5.22.16.0: Legacy Just Works bonding completed, the HID service connected, the light became solid, and button reports were received. After a reboot, stored-key re-encryption succeeded. During 2048 play, controller power-off released all keys and displayed `PAD DISCONNECTED`; powering on without using the Pair button reconnected, and board OK resumed working controls. One in-game reconnect was tested; repeated reconnect and endurance remain untested. |
| IINE-1001 mini controller and board Start/Select fallback | Passed focused device test on 2026-09-24 | A PC capture in pairing mode showed BLE HID service `0x1812` and gamepad appearance `0x03C4`. The user confirmed controller pairing and D-pad input. An ESP-IDF 5.5.3 application-only build was written to COM14 at `0x10000`, with the write hash verified. The user then confirmed that short-pressing board OK sends Game Boy Start and enters the game from its title screen; short board Up for Select and long board OK for pause were also reported working. This was one session; reconnect and endurance with this controller remain untested. |
| Controller pause navigation and ROM capacity | Focused device check passed on 2026-09-24 | The pause menu and its Settings page now accept controller D-pad, A, and B with edge detection; a held button on entry or reconnect does not activate a menu item. The ROM upload limit is derived from the actual partition, preserving the first 4 KiB and two final 4 KiB directory sectors. The current 4 MiB ROM partition permits an aggregate 4,177,920 bytes after 16 KiB bank alignment. Host tests cover near-full upload, refusal without erasing existing data, and directory reopen. The 2,509,968-byte application was flashed to COM14 at `0x10000` on 2026-09-24; esptool verified the written hash. The user confirmed controller Up/Down, A and B work in the pause menu and Settings page, and the management web page shows about 3.98 MiB. A near-full real-device upload and reconnect endurance remain untested. |
| Display and speed | Prior device checks passed; revised heavy-game behavior pending | Initial enlarged Pokemon Blue playback was about 15–20 emulated FPS; direct Flash mapping, tile rendering, O3, unchanged-strip skipping, and a display worker improved tested scenes. Earlier serial intervals measured 57.9 and 59.7 emulated FPS, then 54.2; an animation/save interval measured 49.6. The user later reported slowdowns in other complex games. This build switches between drawing every second and every third emulated frame based on 120-frame work-time windows, and logs core/audio time and display drops. The host policy test passed. A three-phase fixed-input benchmark of the installed Kirby ROM measured drawn-frame core time falling from 20.3/20.0/19.6 ms to 17.8/17.4/16.6 ms with opcode jump tables; skipped-frame time fell from 14.6/11.7/9.6 ms to 12.7/9.9/7.7 ms. A separate instruction-fetch fast path did not improve the benchmark and was removed. Actual complex scenes previously ran about 35–40 emulated FPS; final gameplay speed awaits feedback, and original speed cannot be guaranteed in every scene. |
| Enlarged game image | Visual and controls passed | Game Boy 160×144 is uniformly scaled to 240×216 and centered on the 240×320 LCD. The user confirmed Pokemon Blue image and controls after successive optimizations. A 40-row combined-transfer experiment produced a reported brief stall and was reverted to 20-row transfers. The final display worker copies the 2bpp source frame, so emulation can run while SPI DMA sends the previous displayed frame. Only the app partition at `0x10000` was updated; esptool verified each write hash without overwriting ROMs, saves, or pairing data. |
| Save recovery | Normal device restart passed; revised deletion pending | The user confirmed Pokemon Blue battery-save restoration. The pause page now explains that this is in-game cartridge RAM, loaded automatically on the next launch rather than an arbitrary screen snapshot, and offers Settings. Deleting a ROM removes both save copies unless another identical ROM shares the SHA-256. Startup and game management prune orphan saves only when the ROM catalog is trusted. Host cleanup tests passed; actual deletion and interrupted-write recovery remain untested. |
| Chinese shell and settings | Font readability confirmed; new screens pending visual check | The user reported the 16×16 Noto Sans SC Bold 700 bitmap text is much clearer. Home title is the Game Boy emulator title; the ROM list footer shows long-press OK to go back. Loading, error, and pause states use the Chinese graphical screen, and the game image has a persistent bottom hint. Settings key instructions now appear only in the bottom bar, which scrolls long text without redrawing the whole page. The revised UI built and its font coverage test passed; these newest screen transitions still need a physical check. Battery state and physical up/down/OK hints are rendered. Volume, brightness, and sound switch persist in NVS. |
| Wi-Fi game management | Open AP, readable LCD, fast page opening, upload, and Chinese rename confirmed | The user confirmed FoloToy-GB association, four legible LCD rows, and that the redesigned mobile page opens within seconds with storage, game list, and upload controls. A real game upload worked. An existing game was renamed in Chinese and the user confirmed the name on both the web page and device list. Names use up to 31 UTF-8 bytes; the upload form also offers an editable name, and the LCD font includes the supported BMP glyphs without changing the 72-byte ROM directory entry. Host name tests, firmware image verification, and app-only flashing passed. Direct upload with a Chinese filename, rename persistence across reboot, and the revised game-plus-save deletion remain unverified. |
| Audio | Revised Kirby playback awaits listening check | The user confirmed that stretching one audio block over each slow emulated frame slightly reduced stutter but still stuttered and distorted sound; that approach was removed. The current COM14 build retains 14 kHz output and four-block prebuffer, generating extra fixed-duration APU blocks against wall time instead of stretching pitch. Host pacing tests and firmware build passed, and the app-only flash hash was verified. The user reports Kirby is much smoother overall and pitch is no longer distorted, but rapid or simultaneous button presses still coincide with stutter. Fixed-input jump-table timing proved lower core cost. The user then reported that music is smoother but still alternates between faster and slower tempo, so stable audio tempo has not passed acceptance. Music-register writes depend on emulated game execution, while previous complex scenes ran only about 35–40 emulated FPS, below the Game Boy's roughly 60 FPS. Previous rapid-input logs showed roughly 22–25 ms core time and 2–3 ms audio time per frame, with no display or audio queue drops. The final build also counts audio-task waits to distinguish playback starvation from slower game logic. Slow game logic may delay note changes. An earlier Pokemon Blue 14 kHz load test ran 64 seconds without dropped blocks but did not represent every game. |
| GBC and GBA | Full compatibility not enabled | The CGB-only 32 KiB SRAM probe failed allocation in one run; a prior run left roughly 9 KiB heap with audio active and ran about 32 emulated FPS even without LCD transfer. CGB-compatible cartridges can still run in DMG mode. GBA's baseline 384 KiB EWRAM/IWRAM/VRAM plus emulator and Bluetooth/LCD/audio exceed available internal RAM, and the 4 MiB ROM partition is restrictive. |
| Stability | Partial device check | Three consecutive pause/exit/re-entry cycles completed without reboot, hang, save error, or input failure. A 30-minute run, 20 cycles, 10 reconnects, and heap low-water analysis remain untested. |
| Upstream complete validation gate | Blocked | At the time of this historical check, the source archive had no `.git`, which blocked `tools/check_repo.py`; a local repository was initialized later for publication. The default Bash command resolves to unusable WSL here, while an alternate Bash stops at unsupported MSYS/Mingw and missing `python3`. Focused host tests and native PowerShell ESP-IDF checks ran separately. |

MBC3 RTC does not advance during power-off. The earlier 2048 + Pokemon Blue ROM image was verified byte-for-byte at import time; the user confirmed ordinary Pokemon Blue saves before this update. These reports do not establish the revised pause/settings flow, save deletion, or music quality and speed in specific games.

## Alternative core assessment (2026-09-17)

The [Peanut-GB](https://github.com/deltabeard/Peanut-GB) and [Walnut-CGB](https://github.com/Mr-PauI/Walnut-CGB) MIT cores were compiled into temporary diagnostic applications and run on COM14 against ROMs already installed on the device. Tests used direct Flash mapping, 150 emulated frames per ROM, 30 warm-up frames, no audio output or LCD transfer, and a pixel-line callback to reject misleading speed numbers. These measurements represent startup scenes, not gameplay frame rates.

Both temporary integrations failed to progress on the installed 256 KiB Kirby ROM: across 150 emulated frames only 143 of the expected roughly 21,600 visible lines were delivered, and the CPU ended at `PC=0x0038` with the LCD disabled. The ROM callback logged no out-of-range reads. The cause may be in core compatibility or the temporary adaptation and has not been isolated. Their apparent ~7.5 ms/frame result for Kirby is therefore invalid and cannot justify replacing the working core. Walnut-CGB continued producing lines with the installed Pokemon Blue, Digimon, and Rainbow Fighter ROMs; this does not prove full playability, audio behavior, or save compatibility. The current core's Kirby startup benchmark was about 12.4–12.7 ms without drawing and 18.5–18.7 ms with drawing. Moving its hot CPU and bus methods to IRAM, and simplifying DIV-counter arithmetic, did not yield a repeatable speed gain; both experiments were reverted.

The production firmware remains on the current core. Achieving the original Game Boy tempo in complex Kirby scenes remains unverified and may require a validated replacement core or hardware with more CPU and RAM. ESP32-S3 results from other projects do not establish ESP32-C3 performance.

## HBlank polling fix (2026-09-17)

The installed Rainbow Fighter ROM reproduced a permanent white frame in a host runner: bank 1, PC `0x7f27`, polling `STAT & 3` for mode 0. Previously the core executed visible-line instructions only in mode 3. The revised frame loop executes CPU time in mode 2 (80 dots), mode 3 (172 dots), and mode 0 (204 dots); HALT fast-forward stops at the next phase boundary. Instruction overshoot is carried between phases and lines. This is a fixed-phase compatibility correction, not a cycle-accurate PPU implementation.

The synthetic polling regression failed before the fix and passed afterward. Timing (polling, active CPU frame duration, halted CPU frame duration), DMG renderer, CGB renderer, and partition-layout host checks passed. Both the original and revised runners used ROM data read from the device without writing ROM/save partitions. Rainbow Fighter progressed past its polling loop with the fix. A host run of Digimon displayed its title and progressed after scripted START input, including APU generation and alternate-frame rendering; the user's on-device white-screen symptom is not yet isolated. These checks do not establish full gameplay compatibility or a performance improvement.

The app-only image was flashed at `0x10000` on COM14 with esptool hash verification; startup reached BLE scanning. The user subsequently confirmed that both Rainbow Fighter and Digimon can be played normally on the device; the reported white-screen failures are resolved in this build. This does not establish full-game completion or isolate Digimon's original root cause. The complete shell gate could not run because the available Bash entry resolved to WSL without `/bin/bash`; targeted native checks above were used. Initial merged-image verification encountered a stale output filename, then a relative output-path error; regenerating the exact full-image filename in the build directory and rerunning the verifier passed.

Live logs from the first timing-fix build recorded Rainbow Fighter running at 46.9–53.2 emulated frames/s with input changes, no display or audio queue drops, and intermittent audio queue waits. They also exposed continuous BLE scanning after controller connection. The final build stops scanning once a controller is ready and resumes after disconnection only when the application permits scanning (Wi-Fi management can keep it disabled). Requests are coalesced and evaluated on the Bluetooth thread against current connection state. The final app was built, merged, verified, and flashed; logs show controller readiness, a scan-state change, and Rainbow Fighter execution. The scan policy's disconnect/reconnect behavior and any performance improvement remain to be validated; these runs were not a controlled performance comparison.

## Mapped ROM and native audio optimization (2026-09-17)

Mapped ROM reads now use two cached bank pointers, refreshed on ROM load, reset, state import, and mapper writes. This avoids repeating bank-address calculations for every ROM read; streamed ROM reads retain their previous path. The APU now generates 234 samples per channel at 14 kHz directly, matching the existing output rate and bypassing block resampling. Wall-clock audio scheduling is unchanged. An additional inline opcode-fetch experiment increased core time by about 18–22% versus baseline and was reverted.

Automatic COM14 comparisons used the installed Kirby ROM, stopped BLE scanning, and omitted LCD transfers and audio playback. Each run started a fresh session and executed 450 frames, measuring frames 150–449; START was held at frames 60–79, A at frames >=150 where frame modulo 60 <15, and Right at frames >=200 where frame modulo 120 <80. Every tenth frame yielded one RTOS tick, outside measured core/APU intervals. These numbers isolate compute costs; they are not gameplay FPS or a measurement of audio underruns.

| Draw interval | Baseline core µs/frame | Final core µs/frame, run 1 / repeat | Baseline APU µs/block | Final APU µs/block, run 1 / repeat |
| --- | ---: | ---: | ---: | ---: |
| Every frame | 15735 | 15335 / 15329 | 611 | 538 / 540 |
| Every 2 frames | 11592 | 11103 / 11097 | 612 | 538 / 534 |
| Every 3 frames | 10229 | 9702 / 9700 | 613 | 535 / 536 |

All three on-device rendered-frame sequence hashes matched baseline (`510a4dd6`, `623144cc`, `42e01764`). Five installed ROMs also matched their previous final framebuffers in the 1800-frame host scripted-input runner. Seven targeted host executables/scripts passed, including mapper/reset/fetch-boundary checks, PPU timing, DMG/CGB rendering, continuous-tone frequency, audio pacing, and partition layout. Audio tests statically check agreement between APU and application sample counts/rates. Temporary boot-time benchmark code was removed before the final build; final build, merged-image verification, and app-only flash/hash verification passed. Logs are local ignored `build/perf_rom_*.log` artifacts. Final gameplay logs recorded Kirby at 54.0 and 51.5 emulated frames/s in two ten-second windows with no display/audio queue drops. The user reported it felt much smoother after the requested play test. This supports retaining the iteration but does not establish stable original-speed complex scenes or separately confirm elimination of music-tempo variation.
